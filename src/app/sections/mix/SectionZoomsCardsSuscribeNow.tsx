import SectionSuscribeNow from "../suscribeNow";
import SectionZoomCards from "../zoomCards";

const SectionZoomCardsSuscribeNow = () => {
    return (
        <section>
            <SectionZoomCards />
            <SectionSuscribeNow />
        </section>
    );
}

export default SectionZoomCardsSuscribeNow;